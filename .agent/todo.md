# Task: Random Number Script

- [x] Create `random.sh` with shebang line
- [x] Add input validation (exactly two args, both valid integers)
- [x] Implement random number logic between min and max (inclusive)
- [x] Handle edge case where min > max
- [x] Output the random number to stdout
- [x] Make the script executable with `chmod +x`
