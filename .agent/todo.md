# Task: Random Number Script

- [x] Create `random.sh` with two positional CLI arguments
- [x] Add input validation (exactly two numeric args)
- [x] Add range validation (min <= max)
- [x] Implement random number generation (inclusive, supports large ranges and negatives)
- [x] Make the script executable (chmod +x)
- [x] Verify the script works with `./random.sh 1 10`
