# Task: Random Number Script

- [x] Create `random.sh` with two positional arguments ($1 lower, $2 upper)
- [x] Validate exactly two arguments are provided and both are integers
- [x] Validate lower bound is less than or equal to upper bound
- [x] Generate a random number between the two bounds (handle large ranges)
- [x] Make the script executable with appropriate permissions
